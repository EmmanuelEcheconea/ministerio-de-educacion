import React from "react";

const AbrirLinkBrowser: any = (urlParaBrowser: string) => {
  const abrirLink = (urlParaBrowser: string) => {};

  return {
    handlers: { abrirLink },
  };
};
export default AbrirLinkBrowser;
